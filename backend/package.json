{
  "name": "finza-backend",
  "version": "1.0.0",
  "description": "Finza — WhatsApp Finance Bot Backend",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env --experimental-strip-types src/index.ts",
    "start": "node --env-file=.env --experimental-strip-types src/index.ts",
    "build": "tsc",
    "start:prod": "node dist/index.ts",
    "test": "node --test --experimental-strip-types 'tests/**/*.test.ts'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "pino": "^9.2.0",
    "pino-pretty": "^11.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.6",
    "typescript": "^5.5.3"
  }
}